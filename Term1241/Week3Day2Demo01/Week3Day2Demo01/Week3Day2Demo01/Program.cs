/* Purpose: Assignment 1
 * Author: Harsimranjot
 * Instructor Name: Name of your instructor
 * 
 * */

namespace Week3Day2Demo01
{
    internal class Program
    {
        static void Main(string[] args)
        {
            /*
            // Declaring variable
            float a, b, r1,r2,r3,r4,r5;
            //Taking inputs from user
            Console.Write("Enter first value ");
            a = float.Parse( Console.ReadLine() );

            Console.Write("Enter second value ");
            b = float.Parse(Console.ReadLine());

            // Processing
            r1 = a + b;
            r2 = a - b;
            r3 = a * b;
            r4 = a / b;
            r5 = a % b;

            // Output back to user
            Console.WriteLine($" {a} + {b} = {r1}");
            Console.WriteLine($" {a} - {b} = {r2}");
            Console.WriteLine($" {a} * {b} = {r3}");
            Console.WriteLine($" {a} / {b} = {r4}");
            Console.WriteLine($" {a} % {b} = {r5}");
            */

            /*
            Console.WriteLine(4 + 18 / 6 / 2);

            // What is the largest possible number with 2 digits

            double a = 9, b = 9, c;
            double result = 6.78495;
            
            // Pow(a , b) - a raise to power b
            c = Math.Pow(a, b);

            a += b;
            // a = a + b;

            Console.WriteLine("Result " + c);

            Console.WriteLine("Squareroot of 25= " + Math.Sqrt(25));

            Console.WriteLine(" Result " + Math.Round(result, 2));

            */

            // How to declare constants

            const double Max_Marks = 100;
            const double Number_Students = 101;

            Console.WriteLine($" Max Marks = {Max_Marks} ");
            
        }
    }
}
